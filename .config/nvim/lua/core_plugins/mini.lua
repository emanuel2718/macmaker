-- targets.vim replacement
require("mini.ai").setup({})

-- highlight and act on indent scope
-- require("mini.indentscope").setup({
--   draw = {
--     -- disable animation
--     delay = 0,
--     animation = function()
--       return 0
--     end,
--   },
-- })
